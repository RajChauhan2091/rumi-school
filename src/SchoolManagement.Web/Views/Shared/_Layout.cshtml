<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Rumi Nursery School</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/SchoolManagement.Web.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.5/css/dataTables.bootstrap5.min.css" />
    <link rel="stylesheet" href="https://cdn.datatables.net/buttons/2.4.1/css/buttons.bootstrap5.min.css" />
    @await RenderSectionAsync("Styles", required: false)
</head>
<body>
    @if (User.Identity?.IsAuthenticated == true)
    {
        <div class="app-wrapper">
            <!-- Sidebar -->
            <aside class="app-sidebar" id="sidebar">
                <div class="sidebar-header d-flex flex-column align-items-center text-center py-3">
                    <img src="~/images/rumi_logo.png" alt="Rumi Nursery School" style="max-width: 85%; height: auto; max-height: 80px; object-fit: contain; margin-bottom: 0.25rem;" />
                    <span class="sidebar-subtitle">Management Portal</span>
                </div>
                <ul class="sidebar-menu">
                    <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Dashboard" ? "active" : "")">
                        <a class="menu-link" asp-controller="Dashboard" asp-action="Index">
                            <i class="bi bi-speedometer2"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Students" ? "active" : "")">
                        <a class="menu-link" asp-controller="Students" asp-action="Index">
                            <i class="bi bi-people-fill"></i>
                            <span>Student Directory</span>
                        </a>
                    </li>
                    
                    @if (User.IsInRole("Administrator"))
                    {
                        <li class="menu-header">Setup & Config</li>

                        <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Divisions" ? "active" : "")">
                            <a class="menu-link" asp-controller="Divisions" asp-action="Index">
                                <i class="bi bi-grid-3x3-gap-fill"></i>
                                <span>Divisions</span>
                            </a>
                        </li>
                        <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Classes" ? "active" : "")">
                            <a class="menu-link" asp-controller="Classes" asp-action="Index">
                                <i class="bi bi-journal-bookmark-fill"></i>
                                <span>Classes</span>
                            </a>
                        </li>
                        <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "ClassSchedules" ? "active" : "")">
                            <a class="menu-link" asp-controller="ClassSchedules" asp-action="Index">
                                <i class="bi bi-clock-history"></i>
                                <span>Class Schedules</span>
                            </a>
                        </li>
                        <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Semesters" ? "active" : "")">
                            <a class="menu-link" asp-controller="Semesters" asp-action="Index">
                                <i class="bi bi-calendar-event-fill"></i>
                                <span>Semesters</span>
                            </a>
                        </li>
                        <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Staff" ? "active" : "")">
                            <a class="menu-link" asp-controller="Staff" asp-action="Index">
                                <i class="bi bi-person-badge-fill"></i>
                                <span>Staff Management</span>
                            </a>
                        </li>
                        <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Fees" && ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "active" : "")">
                            <a class="menu-link" asp-controller="Fees" asp-action="Index">
                                <i class="bi bi-wallet2"></i>
                                <span>Fee Configuration</span>
                            </a>
                        </li>
                        <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Fees" && ViewContext.RouteData.Values["action"]?.ToString() == "Mappings" ? "active" : "")">
                            <a class="menu-link" asp-controller="Fees" asp-action="Mappings">
                                <i class="bi bi-diagram-3-fill"></i>
                                <span>Fee Mapping</span>
                            </a>
                        </li>
                        <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Payments" && ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "active" : "")">
                            <a class="menu-link" asp-controller="Payments" asp-action="Index">
                                <i class="bi bi-cash-coin"></i>
                                <span>Student Payments</span>
                            </a>
                        </li>
                        <li class="menu-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Payments" && ViewContext.RouteData.Values["action"]?.ToString() == "PendingReport" ? "active" : "")">
                            <a class="menu-link" asp-controller="Payments" asp-action="PendingReport">
                                <i class="bi bi-file-earmark-bar-graph-fill"></i>
                                <span>Pending Fees Report</span>
                            </a>
                        </li>
                    }
                </ul>
                <div class="sidebar-footer">
                    <div class="user-profile">
                        <div class="user-avatar">
                            @User.Identity.Name?.Substring(0, Math.Min(2, User.Identity.Name.Length)).ToUpper()
                        </div>
                        <div class="user-info">
                            <span class="user-name">@User.Identity.Name</span>
                            <span class="user-role">@(User.IsInRole("Administrator") ? "Administrator" : "Clerk")</span>
                        </div>
                    </div>
                </div>
            </aside>

            <!-- Main Container -->
            <div class="app-container">
                <header class="app-header">
                    <div class="d-flex align-items-center gap-3">
                        <button class="btn btn-sm border-0 fs-5" onclick="toggleSidebar()" title="Toggle Sidebar" style="color: var(--text-muted);">
                            <i class="bi bi-list"></i>
                        </button>
                        <div>
                            <h5 class="header-title">@ViewData["Title"]</h5>
                        </div>
                    </div>
                    <div class="header-actions">
                        <span class="text-muted d-none d-sm-inline"><i class="bi bi-person-circle me-1"></i> Welcome, <strong>@User.Identity.Name</strong></span>
                        <div class="dropdown">
                            <button class="btn btn-sm btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Account
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end shadow-sm">
                                <li>
                                    <a class="dropdown-item" asp-controller="Auth" asp-action="ChangePassword">
                                        <i class="bi bi-key-fill me-2"></i> Change Password
                                    </a>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <form asp-controller="Auth" asp-action="Logout" method="post">
                                        <button type="submit" class="dropdown-item text-danger">
                                            <i class="bi bi-box-arrow-right me-2"></i> Logout
                                        </button>
                                    </form>
                                </li>
                            </ul>
                        </div>
                    </div>
                </header>

                <main class="main-content">
                    @RenderBody()
                </main>
            </div>
        </div>
    }
    else
    {
        @RenderBody()
    }

    <!-- Offline Alert -->
    <div id="offlineAlert" class="offline-alert" style="display: none;">
        <div class="offline-alert-content">
            <div class="offline-alert-icon">
                <i class="bi bi-wifi-off"></i>
            </div>
            <div class="offline-alert-message">
                <h5>No Internet Connection</h5>
                <p>You are currently offline. Some features may not work properly. Please check your internet connection.</p>
            </div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <!-- DataTables JS & Buttons Extensions -->
    <script src="https://cdn.datatables.net/1.13.5/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.5/js/dataTables.bootstrap5.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.4.1/js/dataTables.buttons.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.4.1/js/buttons.bootstrap5.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/pdfmake.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/vfs_fonts.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.4.1/js/buttons.html5.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.4.1/js/buttons.print.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="~/js/connectivity-check.js" asp-append-version="true"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        $(document).ready(function() {
            var successMsg = '@(TempData["SuccessMessage"] != null ? Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(TempData["SuccessMessage"].ToString())) : "")';
            var errorMsg = '@(TempData["ErrorMessage"] != null ? Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(TempData["ErrorMessage"].ToString())) : "")';
            
            const Toast = Swal.mixin({
                toast: true,
                position: 'top-end',
                showConfirmButton: false,
                timer: 4000,
                timerProgressBar: true,
                didOpen: (toast) => {
                    toast.addEventListener('mouseenter', Swal.stopTimer)
                    toast.addEventListener('mouseleave', Swal.resumeTimer)
                }
            });

            if (successMsg && successMsg !== '') {
                Toast.fire({
                    icon: 'success',
                    title: successMsg
                });
            }
            if (errorMsg && errorMsg !== '') {
                Toast.fire({
                    icon: 'error',
                    title: errorMsg
                });
            }
        });

        function toggleSidebar() {
            if (window.innerWidth >= 992) {
                document.body.classList.toggle('sidebar-collapsed');
            } else {
                var sidebar = document.getElementById('sidebar');
                if (sidebar) {
                    sidebar.classList.toggle('show');
                }
            }
        }

        // Close sidebar on mobile when overlay area is clicked
        document.addEventListener('click', function(e) {
            if (window.innerWidth < 992) {
                var sidebar = document.getElementById('sidebar');
                if (sidebar && sidebar.classList.contains('show') && !sidebar.contains(e.target) && !e.target.closest('[onclick*="toggleSidebar"]')) {
                    sidebar.classList.remove('show');
                }
            }
        });
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
